const router = {
    root: 'root',
    navigation: 'navigation',
    setting: 'setting',
    config: 'config',
}

const inputer = {
    split: '___',
    args: '{}',
}

export {
    router, inputer
};